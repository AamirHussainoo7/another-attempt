pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/AamirHussainoo7/another-attempt.git'  // Replace with your actual repository URL
            }
        }

        stage('Build') {
            steps {
                echo 'No build steps needed for static files.'
            }
        }

        stage('Test') {
            steps {
                echo 'No tests defined. Add HTML/CSS linters if needed.'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying static files...'
                // Example: Copy files to a web directory
                sh 'cp -r * /var/www/html/'  // Adjust as needed for your server
            }
        }
    }
}
